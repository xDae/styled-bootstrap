// @flow

export const closeFontSize = '1.5rem';
export const closeFontWeight = 700;
export const closeColor = '#000';
export const closeTextShadow = '0 1px 0 #fff';

const defaultTheme = {
  closeFontSize,
  closeFontWeight,
  closeColor,
  closeTextShadow
};

export default defaultTheme;
